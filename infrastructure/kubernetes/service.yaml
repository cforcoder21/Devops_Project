apiVersion: v1
kind: Service
metadata:
  name: postmortem-service
  labels:
    app: postmortem-app
spec:
  selector:
    app: postmortem-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
